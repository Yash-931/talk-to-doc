[project]
name = "talk-to-doc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yash-931", email = "yaggarwal931@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "nltk>=3.10.3",
    "pdfplumber>=0.11.10",
    "pydantic>=2.13.5",
    "tiktoken>=0.14.0",
]

[project.scripts]
talk-to-doc = "talk_to_doc:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
